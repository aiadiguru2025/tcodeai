# Database (Neon PostgreSQL)
DATABASE_URL="postgresql://username:password@host.neon.tech/tcodeai?sslmode=require"

# OpenAI API
OPENAI_API_KEY="sk-..."

# Redis (Upstash) - Optional for caching
UPSTASH_REDIS_REST_URL="https://..."
UPSTASH_REDIS_REST_TOKEN="..."

# App
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# LLM Judge Configuration (Optional)
# JUDGE_MODEL="gpt-4o-mini"        # Model for validation (default: gpt-4o-mini)
# JUDGE_TIMEOUT_MS="5000"          # Timeout in milliseconds (default: 5000)
# JUDGE_TEMPERATURE="0.1"          # Temperature 0-1 (default: 0.1)
# DISABLE_LLM_JUDGE="false"        # Set to 'true' to disable judge validation

# Web Search APIs (for enhanced low-confidence fallback)
SERPAPI_API_KEY="..."              # SerpAPI for Google Search (get key at serpapi.com)
BRAVE_SEARCH_API_KEY="..."         # Brave Search API (optional, get key at brave.com/search/api)
